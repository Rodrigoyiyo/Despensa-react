import React from 'react';
import ReactDOM from 'react-dom/client';
import { AppDespensa } from './AppDespensa';

import './colorBootstrap.css';
import './styles.css';


ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <AppDespensa />
    </React.StrictMode>
)